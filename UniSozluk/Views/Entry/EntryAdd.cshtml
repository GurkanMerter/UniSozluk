@model EntityLayer.Concrete.Entry

@{
    ViewData["Title"] = "EntryAdd";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="col-lg-10 col-md-11">
    <div class="card mb-4">
        <div class="card-header">
            Entry Oluşturma
        </div>
        <form class="container-fluid mt-2">
            <div class="form-group">
                <label for="exampleFormControlInput1">Üniversite:</label>
                <select class="form-control" disabled>
                    <option>Default select</option>
                </select>
            </div>
            <div class="form-group">
                <label for="exampleFormControlInput1">Bölüm:</label>
                <select class="form-control">
                    <option>Default select</option>
                </select>
            </div>
            <div class="form-group">
                <label for="exampleFormControlInput1">Entry İçeriği:</label>
                <textarea class="form-control"></textarea>
            </div>
            <a href="#" class="btn btn-success btn-icon-split mb-3 float-right">
                <span class="icon text-white-50">
                    <i class="fas fa-check"></i>
                </span>
                <span class="text">Split Button Success</span>
            </a>
        </form>
    </div>

</div>
